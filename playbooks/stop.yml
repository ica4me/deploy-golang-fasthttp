---
- name: Stop all managed FastHTTP containers
  hosts: fasthttp_backends
  become: true
  gather_facts: false
  tasks:
    - name: Stop containers
      community.docker.docker_container:
        name: "fasthttp-{{ item }}"
        state: stopped
      loop: "{{ range(1, (backend_container_count | int) + 1) | list }}"
